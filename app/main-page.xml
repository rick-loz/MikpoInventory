<!--
The markup in NativeScript apps contains a series of user interface components, each
of which NativeScript renders with a platform-specific iOS or Android native control.
You can find a full list of user interface components you can use in your app at
https://docs.nativescript.org/ui/components.
-->
<Page xmlns:lv="nativescript-ui-listview" xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo" class="page">
    <!--
    The ActionBar is the NativeScript common abstraction over the Android ActionBar and iOS NavigationBar.
    http://docs.nativescript.org/ui/action-bar
    -->
    <Page.actionBar>
        <ActionBar title="Inventario" icon="" class="action-bar">
        </ActionBar>
    </Page.actionBar>
    <!--
    The StackLayout stacks UI components on the screen—either vertically or horizontally.
    In this case, the StackLayout does vertical stacking; you can change the stacking to
    horizontal by applying a orientation="horizontal" attribute to the <StackLayout> element.
    You can learn more about NativeScript layouts at
    https://docs.nativescript.org/ui/layout-containers.

    These components make use of several CSS class names that are part of the NativeScript
    core theme, such as p-20, btn, h2, and text-center. You can view a full list of the
    class names available for styling your app at https://docs.nativescript.org/ui/theme.
    -->
    <StackLayout>
        <lv:RadListView id="listView" class="listView" items="{{ items }}" itemTap="onItemTap" height="50%">
            <lv:RadListView.listViewLayout>
                <lv:ListViewGridLayout scrollDirection="Vertical" spanCount="2" />
            </lv:RadListView.listViewLayout>
            <lv:RadListView.itemTemplate>
                <StackLayout orientation="vertical" class="items">
                    <Image src="{{ 'res://' + img }}" height="180" stretch="aspectFill"/>
                    <Label fontSize="20" text="{{ name }}" class="itemInfo itemName"/>
                    <Label fontSize="14" text="{{ 'Quantity: ' + quantity + '/' + maxQuantity}}" class="itemInfo"/>
                    <Label fontSize="14" text="{{ 'Price: $' + price }}" class="itemInfo"/>
                </StackLayout>
            </lv:RadListView.itemTemplate>
        </lv:RadListView>
        <GridLayout class="selected" rows="*,2*,2*,2*,3*,1*" columns="2*,*,2*" height="45%">
            <Label fontSize="17" text="{{ 'Item selected: ' + currName }}" row="0" colSpan="2" class="itemSelected"/>
            <Label fontSize="17" text="{{ 'Item Price: $' + currPrice }}" row="0" col="2" class="itemSelected"/>
            <Button text="-" tap="onQuantityTap" type="less" row="1" col="0" class="less"/>
            <TextField id="selectedQuantity" hint="Enter Quantity..." text="{{ currQuantity  }}" autocorrect="false" keyboardType="number" returnKeyType="next" row="1" col="1"/>
            <Button text="+" tap="onQuantityTap" type="more" row="1" col="2" class="more"/>
            <Button text="Sold With Cash" tap="onSoldTap" type="cash" row="2" colSpan="3" class="sold"/>
            <Button text="Sold With Card" tap="onSoldTap" type="card" row="3" colSpan="3" class="sold"/>
            <Button text="Undo Last" tap="onUndoTap" row="4" colSpan="3" class="undo"/>
            <Label fontSize="15" text="{{ errorMessage }}" row="5" colSpan="3" class="error"/>
        </GridLayout> 
        <GridLayout columns="*,*" height="5%">
            <Label fontSize="15" text="{{ 'Total Sales: $' + totalSales }}" class="total"/>
            <Label fontSize="15" text="{{ 'Total Transactions: ' + totalTransactions }}" col="1" class="total"/>
        </GridLayout>
    </StackLayout>
</Page>
